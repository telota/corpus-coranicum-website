<locus>
    <from sura="{{ $passage->sura_start }}" verse="{{ $passage->verse_start }}" word="{{ $passage->word_start }}"/>
    <to sura="{{ $passage->sura_end }}" verse="{{ $passage->verse_end }}" word="{{ $passage->word_end }}"/>
</locus>
